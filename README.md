# financial

บริหารงานงบประมาณ — ระบบบริหารงบประมาณโรงเรียน

ระบบครอบคลุม 4 งานที่ใช้งบประมาณก้อนเดียวกัน และทยอยพัฒนาเป็นเฟส

| งาน | ขอบเขต | เฟส |
| --- | --- | --- |
| 01 งานแผนงาน | ตั้งแผนปฏิบัติการ · จัดสรรงบตามโครงการ · อนุมัติคำขอซื้อ | เฟส 3 |
| 02 งานพัสดุ | จัดซื้อจัดจ้าง · ทะเบียนครุภัณฑ์ · ตรวจสอบประจำปี · จำหน่ายพัสดุ | **เฟส 1 — กำลังพัฒนา** |
| 03 งานการเงิน | เบิกจ่าย · ใบสำคัญ · ยืมเงินราชการ · คุมเงินคงเหลือรายหมวด | เฟส 2 |
| 04 งานบัญชี | ลงบัญชีสินทรัพย์ · ค่าเสื่อมราคา · งบการเงิน · รายงานต้นสังกัด | เฟส 4 |

## เฟส 1 — แบบสำรวจครุภัณฑ์โดยครู

Next.js (App Router) + Supabase — ฐานข้อมูลเดียว ใช้ร่วมกันทั้ง 2 หน้าจอกรอกข้อมูล

| หน้า | ใช้เมื่อ | รายละเอียด |
| --- | --- | --- |
| `/survey` | มือถือ เดินสำรวจในห้อง | ทีละขั้น 4 ขั้น **ถ่ายรูปนำ** → ที่ตั้ง → ตัวครุภัณฑ์ → สภาพ · จำห้องล่าสุด · บันทึกแล้วส่งให้งานพัสดุทันที เพิ่มชิ้นถัดไปในห้องเดิมได้ต่อเนื่อง |
| `/review` (เจ้าหน้าที่พัสดุ) | ตรวจสอบรายการที่ส่งเข้ามา | ดูรายการทั้งหมด ออกหมายเลขครุภัณฑ์ อนุมัติ/ตีกลับ แก้ไขข้อมูลฝั่งจัดซื้อ พิมพ์ทะเบียนคุมทรัพย์สิน |

- เอกสารออกแบบต้นทาง: [design/phase1-asset-survey](design/phase1-asset-survey/)
- ฟิลด์บังคับ 5 อย่าง: ห้อง · ชื่อครุภัณฑ์ · จำนวน · สภาพ · รูป
  (ปี/แหล่งงบ/ราคา พับเก็บใต้ "ข้อมูลเพิ่มเติม")
- ระบบกันหมายเลขครุภัณฑ์ซ้ำภายในรอบเดียวกันด้วย unique index ที่ฐานข้อมูล (บันทึกซ้ำจะฟ้อง error ทันที)
- ถ่ายรูปเป็นขั้นแรกของฟอร์มเสมอ กรอกครบแล้วส่งเข้าระบบทันที ("submitted") ไม่มีสถานะร่างค้างไว้ให้กลับมาแก้

### การเข้าใช้งาน

ครูไม่ต้องล็อกอินเลย — เปิดลิงก์ `/survey` แล้วกรอกได้ทันที เหมือน Google Form
ไม่ต้องสมัคร ไม่ต้องกดปุ่มเข้าใช้งานใด ๆ ไม่มี `auth.uid()` ผูกกับผู้กรอก
รายการที่กรอกจึง **ไม่เกี่ยวข้องกับตัวครูผู้ตอบ** — ส่งแล้วส่งเลย แก้ไขย้อนหลังทำได้เฉพาะฝั่งเจ้าหน้าที่พัสดุที่หน้า `/review`

- เจ้าหน้าที่พัสดุ/ผู้ดูแลระบบเท่านั้นที่ต้องล็อกอินด้วยอีเมล+รหัสผ่าน (หน้า `/login`)
  เพื่อเข้าหน้า `/review` และ `/admin`

### รูปถ่ายครุภัณฑ์

รูปเก็บใน Supabase Storage bucket `asset-photos` (bucket ปิด เข้าถึงผ่าน signed URL อายุ 1 ชม.)

- **บีบอัดฝั่งเบราว์เซอร์ก่อนอัปโหลดเสมอ** — ย่อด้านยาวสุดเหลือ 1600px แล้วบันทึกเป็น JPEG
  คุณภาพ 75% (`src/lib/image.ts`) รูปจากกล้องมือถือที่มักหนัก 3–8 MB จะเหลือหลักร้อย KB
  ก่อนขึ้น — ถ้าเบราว์เซอร์ถอดรหัสไฟล์ไม่ได้ (เช่นบางฟอร์แมต HEIC) จะอัปโหลดไฟล์ต้นฉบับแทน
  ไม่ทำให้พัง
- **อัปโหลดได้แม้ไม่ได้ล็อกอิน** (ครูกรอกแบบสำรวจ) แต่ **ดู/แทนที่/ลบสงวนไว้ให้เจ้าหน้าที่พัสดุ/แอดมินเท่านั้น**
  ไม่มีใครนอกจากทีมพัสดุเปิดดูรูปย้อนหลังได้

### สิทธิ์ผู้ใช้

| บทบาท | สิทธิ์ |
| --- | --- |
| (ไม่ได้ล็อกอิน — ครูผู้กรอกแบบสำรวจ) | เพิ่มรายการใหม่ได้อย่างเดียว (`/survey`) ดู/แก้/ลบรายการไม่ได้เลย |
| `supply` (เจ้าหน้าที่พัสดุ) | เห็น/แก้ได้ทุกรายการ · แก้ไขข้อมูลตั้งต้น (อาคาร หมวดหมู่ แหล่งงบ หน่วย) · เปิด–ปิดรอบสำรวจ |
| `admin` | สิทธิ์ทั้งหมด รวมถึงเปลี่ยนบทบาทผู้ใช้ |

บังคับด้วย Row Level Security ที่ฐานข้อมูล ไม่ใช่ที่หน้าจอ

## ติดตั้งเพื่อพัฒนา

```bash
npm install
cp .env.local.example .env.local   # ใส่ค่า NEXT_PUBLIC_SUPABASE_URL / ANON_KEY
npm run dev
```

### ฐานข้อมูล

รัน migration ใน [`supabase/migrations`](supabase/migrations/) ผ่าน Supabase CLI

```bash
supabase link --project-ref <project-ref>
supabase db push
```

หรือคัดลอกเนื้อไฟล์ไปรันใน SQL Editor ของ Supabase ก็ได้ (เขียนให้รันซ้ำได้ไม่พัง)

Migration จะสร้างให้ครบ: ตาราง `asset_*`, enum, RLS policy, bucket `asset-photos`,
trigger สร้างโปรไฟล์ให้ผู้ใช้ใหม่ และข้อมูลตั้งต้น (อาคาร/หมวดหมู่/แหล่งงบ/หน่วย + รอบปีการศึกษา 2569)

> ตารางทั้งหมดใช้ prefix `asset_` เพื่อให้ติดตั้งร่วมกับโปรเจกต์ Supabase เดิมได้โดยไม่ชนชื่อ

หลังรัน migration ให้ตั้งเจ้าหน้าที่พัสดุคนแรกด้วย

```sql
update public.asset_survey_profiles set role = 'admin' where user_id = '<uuid ของผู้ใช้>';
```

### สภาพแวดล้อมที่ใช้อยู่

| อย่าง | ค่า |
| --- | --- |
| โปรเจกต์ Supabase | `financial-asset-survey` (ap-southeast-1) — แยกจากโปรเจกต์อื่น |
| Migration | รันแล้วทั้งหมด (ตาราง · RLS · bucket · ข้อมูลตั้งต้น) |
| ข้อมูลในระบบ | ว่าง — เหลือแต่ข้อมูลตั้งต้นและรอบปีการศึกษา 2569 |

### เรื่องที่ต้องตั้งค่าก่อนให้ครูใช้จริง

1. **บัญชีอีเมล (เฉพาะเจ้าหน้าที่)** — Supabase บังคับยืนยันอีเมล และ SMTP ในตัวจำกัด
   การส่งไว้ไม่กี่ฉบับต่อชั่วโมง ถ้าจะสร้างบัญชีเจ้าหน้าที่หลายคนควรตั้ง SMTP ของโรงเรียน
   ที่ Authentication → Emails ก่อน (ครูทั่วไปไม่ต้องใช้อีเมลอยู่แล้ว)
2. **ใครก็เปิดลิงก์แล้วกรอกได้** — `/survey` เปิดสาธารณะไม่มีรหัสกั้นเลย (ตั้งใจให้เหมือน Google Form)
   ถ้าลิงก์หลุดออกไปนอกโรงเรียน คนนอกก็เพิ่มรายการปลอมเข้ามาได้ (ดู/แก้/ลบรายการใครไม่ได้ ทำได้แค่เพิ่ม)
   ถ้าต้องการกั้นภายหลัง ให้เจ้าหน้าที่พัสดุตรวจสอบรายการที่หน้า `/review` ก่อนอนุมัติเสมอ
3. **ผังอาคาร–ห้องจริง** — แก้ตาราง `asset_buildings` ให้ตรงกับโรงเรียน
   (ตอนนี้ใส่ตัวอย่างไว้: อาคาร 1–4 · โรงฝึกงาน · โรงอาหาร · หอประชุม)
4. **รูปแบบหมายเลขครุภัณฑ์** — ตอนนี้เป็นข้อความอิสระ ไม่บังคับรูปแบบ
   (แค่กันเลขซ้ำ) ถ้าโรงเรียนมีรูปแบบตายตัวควรเพิ่ม validation

## โครงสร้างโค้ด

```
src/
  app/            หน้าเว็บ (App Router) — page.tsx, login, survey, (staff)/review, (staff)/admin, (staff)/items
  components/     ส่วนประกอบร่วม — ฟอร์ม, ชิป, ปุ่มจำนวน, ช่องถ่ายรูป, toast
  lib/
    supabase/     client (เบราว์เซอร์) และ server (server component)
    data.ts       ชั้นเดียวที่คุยกับฐานข้อมูล — ใช้ร่วมกันทุกหน้าจอ
    hooks.ts      โหลด master data · โปรไฟล์ · จำห้องล่าสุด (localStorage)
    types.ts      ชนิดข้อมูลตรงกับ schema
  proxy.ts        กันเฉพาะหน้าเจ้าหน้าที่พัสดุ/แอดมิน + ต่ออายุ session (/, /survey เปิดสาธารณะ)
supabase/migrations/
```

## สิ่งที่ยังไม่ได้ทำ (เฟส 1 ส่วนที่เหลือ)

- กรอกแบบออฟไลน์ (คิวรออัปโหลดเมื่อสัญญาณกลับมา)

ที่ทำแล้ว: ตรวจสอบ/อนุมัติ/ตีกลับที่ `/review` · ส่งออกบัญชีครุภัณฑ์เป็น PDF (พิมพ์)
และ Excel (`.csv` เปิดใน Excel ได้ตรง ๆ) ที่ `/items/print` · แดชบอร์ดความคืบหน้า
รายอาคาร/มูลค่าสินทรัพย์รวมที่ `/dashboard` · พิมพ์ป้าย QR แทนพิมพ์เลขครุภัณฑ์ด้วยมือ
ที่ `/items/labels` (QR เก็บเลขครุภัณฑ์เป็นข้อความล้วน สแกนด้วยแอปทั่วไปได้เลย) · ครบวงจร
ด้วยปุ่ม "สแกน" ที่ `/survey` ให้ถ่ายรูปป้าย QR (จากรอบสำรวจก่อนหน้า) แทนพิมพ์เลข
เองได้เลยตอนกรอกแบบสำรวจรอบถัดไป
